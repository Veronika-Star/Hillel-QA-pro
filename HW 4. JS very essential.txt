Задача 1: Перевірте, чи рядок не є порожнім, та виведіть відповідне повідомлення.
Рядок задати змінною на початку рішення.

var myString = "Це мій рядок";
if (myString !==  "") {
    console.log("Рядок не є порожнім");
} else {
    console.log("Рядок порожній.");
}


Задача 2: Перевірте, чи введене значення є числом, та виведіть відповідне повідомлення. 
Значення введіть з консолі або задайте змінною на початку рішення

var b = '8f';
if(Number(b)){
    console.log(b + ' - is a number');
}else{
    console.log(b + ' - not a number');
}


Задача 3: Порівняйте два числа та виведіть результат порівняння у вигляді булеану. 
Значення введіть з консолі або задайте змінною на початку рішення

let num1 = prompt("Введіть перше число:");
let num2 = prompt("Введіть друге число:");

let result = num1 == num2;
console.log(result)

